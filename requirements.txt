# Core runtime deps (pip install -r requirements.txt). Or use: uv pip install -e .
httpx>=0.27
tenacity>=8.2
pandas>=2.1
typer>=0.12
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
jinja2>=3.1
# dev
pytest>=8.0
